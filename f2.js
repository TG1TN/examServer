const f1 = require('./f1')//importing single value from f1
const {city, state} = require('./f3')//importing multiple values from f3
const func = require('./f4')//im
console.log(f1, city, state)
console.log(func(10,20))